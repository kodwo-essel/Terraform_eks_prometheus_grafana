apiVersion: v1
kind: Service
metadata:
  name: voting-app
  labels:
    name: voting-app
    app: demo-voting-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5000
      # nodePort: 30005
  selector:
    name: voting-app-pod
    app: demo-voting-app
